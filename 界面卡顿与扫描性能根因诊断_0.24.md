# 轻音 0.24：界面卡顿与本地扫描缓慢根因诊断

**诊断日期：** 2026-08-14  
**范围：** 当前 `main` 分支的 Android 原生实现。  
**方法：** 静态执行路径审查；本次未修改业务源码，也未在用户设备上采集 Perfetto/Macrobenchmark 实测数据。因此，下面的“优先级”是由代码路径、算法复杂度与 Android 存储 API 的行为边界推断得出，须在真实设备上以分段耗时验证。

> **结论：当前问题的首要根因不是 Kotlin 本身，也不是“没有用 Rust”。** 扫描的主耗时来自 SAF 文档树的全量 Binder 枚举和 `MediaMetadataRetriever` 文件级读取；扫描结束后的本地匹配预计算又可能把 CPU 持续占满。界面卡顿则还叠加了封面逐项读取和播放器快照的高频状态发布。直接改写为 Rust 无法替换 `DocumentFile`、MediaStore 或 `MediaMetadataRetriever` 这些 Android 系统边界，第一阶段收益会很有限。

## 一、用户体感的完整耗时链

当前一次“扫描慢/扫描后仍卡”的体验不是单个函数造成，而是连续发生的六段工作：

| 阶段 | 当前行为 | 用户体感 | 代码证据 |
|---|---|---|---|
| 1. 启动触发 | 启动时立即发起扫描；自定义目录设置恢复后还可能再次请求扫描；授权后也会请求扫描。互斥锁会丢弃并发请求，但无法避免先后发生的重复完整扫描。 | 启动后持续转圈，部分设备会感觉“刚扫完又在扫”。 | `MainActivity.kt:95-98, 137-140`；`MainViewModel.kt:175-228, 296-298` |
| 2. 系统媒体库 | 每次扫描均对 MediaStore 发起全量查询，按标题排序，并逐条构造或复用轨道。 | 音乐多时有固定基线耗时，即使没有新增文件也不能做到近乎瞬时。 | `LocalMusicRepository.kt:16-80` |
| 3. 自定义目录（SAF） | 每次扫描均从根目录递归到最多 24 层，对每个目录执行 `DocumentFile.listFiles()`，并读取文件类型/名称。缓存只跳过元数据提取，**不跳过目录枚举**。 | 这是大目录、SD 卡、云盘文档提供者上最可能的第一瓶颈。 | `CustomFolderRepository.kt:19-46` |
| 4. 新文件元数据 | 每个新文件都创建 `MediaMetadataRetriever`，读取标题、艺人、专辑和时长。虽限制为四路并发，但仍是高延迟媒体解析。 | 新增文件很多时，进度长时间无明显反馈；并发过高时还可能增加 I/O 竞争。 | `CustomFolderRepository.kt:15-35, 54-67` |
| 5. 扫描结果处理 | 扫描结果回到 `viewModelScope` 默认的主线程后执行合并、去重、列表相等比较；变化时更新 StateFlow、落盘索引并触发匹配索引重建。 | 大库结束时可能出现“扫描完成瞬间卡一下”。 | `MainViewModel.kt:196-220` |
| 6. 匹配预计算 | 本地索引重建后会对搜索、每日推荐和当前歌单进行匹配；索引无候选时退回全量本地候选做 Levenshtein 评分。 | 扫描提示已结束，但应用仍卡、列表仍在跳动或 CPU 占用很高。 | `MainViewModel.kt:676-736`；`TrackMatcher.kt:69-179, 237-250` |

## 二、按优先级排序的根因

### P0：SAF 自定义目录每次都做全树递归枚举

`CustomFolderRepository.scan()` 每次都调用 `walk()`，而 `walk()` 对树中每个目录执行 `DocumentFile.listFiles()`，并对每个文件读取 `isFile`、MIME 类型或名称。即使 `previous` 中已经有该文件，缓存仅用于避免 `MediaMetadataRetriever`，无法避免 Document Provider 的跨进程调用。

这意味着当用户选择的是大型音乐目录、SD 卡或第三方文件提供者时，冷扫和热扫都接近 **O(目录项数量)**。这种成本主要受 Android DocumentsProvider、存储介质和 Binder 往返影响，而非 Kotlin 字符串或协程本身。因此把递归器改写成 Rust 并不能绕开该边界：Rust 仍必须经 JNI/ContentResolver/DocumentFile 或等价的 DocumentsContract 调用访问同一批文件。

> **判断：这是扫描慢的最高优先级根因。**

### P0：扫描后本地匹配存在“索引未命中即全库评分”路径

`findLocalMatch()` 在索引没有候选时，把 `localPlaybackCandidates()` 的所有本地与下载曲目送入 `TrackMatcher.findBest()`。评分过程中会对每个候选执行标题形式生成、规范化、艺人拆分和字符串相似度；底层相似度为 Levenshtein 动态规划。

当一个页面有 `R` 首线上歌曲、本地库有 `N` 首、平均标题长度为 `L` 时，最坏情况下大致为 **O(R × N × L²)**。虽然预计算放在 `Dispatchers.Default`，但 `findLocalMatch()` 把候选查找和评分整体放在同一把 `localMatchCache` 锁内，多个预计算任务会串行；播放点击路径也会争用这把锁。扫描后如果当前歌单、搜索结果或每日推荐较多，就会表现为“扫描之后还卡很久”。

> **判断：这是扫描结束后持续卡顿的最高优先级根因，也是最值得先重构的纯计算路径。**

### P1：启动和状态恢复可先后触发多次完整扫描

`LaunchedEffect(Unit)` 在启动时扫描，权限回调也扫描；自定义目录设置从 DataStore 恢复后，`LaunchedEffect(settings.customFolderUris)` 再调用 `restoreCustomFolders()` 并尝试扫描。`scanGate` 能阻止并发叠加，但若第一次扫描在设置恢复前结束，第二次调用会再做一次完整枚举。

此外，下载任务结构变化会触发 `downloadedTracks()`、匹配重建，并可能节流后再安排本地重扫。对于下载量多的用户，这会让扫描与下载状态变化交织。

> **判断：这是“有时明显更慢、重启后也扫很久”的重要放大器。**

### P1：首次滚动本地/下载列表会逐项提取内嵌封面

本地与下载曲目行会在 `CachedCoverImage` 的 `LaunchedEffect` 中调用 `ArtworkStore.localEmbeddedArtwork()`。缓存未命中时，此函数会查询 Room，再新建 `MediaMetadataRetriever`，打开媒体 URI 并读取 `embeddedPicture`，然后把图片写入磁盘并写回 Room。

这些工作虽然通过 `Dispatchers.IO` 执行，但一屏多个曲目首次出现时会同时发起多次数据库、媒体读取、图片解码与 Compose 状态更新；低端设备或慢存储下，滚动会出现明显掉帧。

> **判断：这是“页面本身很卡，尤其首屏或首次滚动”的主要原因之一；与扫描速度不同，但用户感受会叠加。**

### P2：播放器每 500ms 更新根部状态

`PlaybackConnection` 每 500ms 发布一次新的 `PlayerSnapshot`，`QingyinApp` 在根部收集该状态。实现已避免每次重建整个队列，但根部组合仍会定期重新执行；在列表、毛玻璃效果、封面加载和扫描状态同时变化时，会放大主线程压力。

Compose 编译器可能跳过参数未变的子组合，因此这不是扫描慢的主因；但它会降低 UI 的余量，使其他 I/O、解码和状态更新更容易体现为卡顿。

> **判断：需要优化，但不应排在 SAF 或匹配全库评分之前。**

## 三、为什么现在不建议先换 Rust

| 考量 | 结论 |
|---|---|
| SAF 目录遍历 | Rust 无法绕过 DocumentsProvider/Binder；仍必须调用 Android ContentResolver/DocumentsContract。收益很小。 |
| MediaStore 查询 | 同样是 Android 系统数据库/ContentProvider 边界；语言切换无法显著缩短查询和 IPC。 |
| `MediaMetadataRetriever` | 属于 Android 媒体框架调用；JNI 跨语言后依然是同一个解析器，且会增加资源释放复杂度。 |
| 字符串匹配与索引 | 这是可迁移的纯 CPU 模块；但应先移除“未命中就全库评分”、做持久化倒排索引和批处理，再看 Rust 是否必要。 |
| 工程风险 | 引入 Rust 需要 NDK、JNI/UniFFI 边界、ABI 构建矩阵、崩溃符号上传和额外测试；在问题主要为 Android I/O 时，成本大于收益。 |

> **推荐结论：先做 Kotlin 架构重构与实测埋点，不启动全量 Rust 改写。** 若在完成 P0 改造后，真实 trace 仍显示“字符串规范化/相似度评分”占据大量 CPU，才把**纯匹配引擎**作为第二阶段 Rust 候选；绝不迁移 MediaStore、SAF 或封面提取本身。

## 四、建议的验证指标（先测再改）

应在真实用户设备上对以下分段打点，并用同一真实音乐库复测。不要用随机造数据替代真实 SAF/MediaStore 行为。

| 指标 | 需要记录的边界 | 目标用途 |
|---|---|---|
| 启动可用时间 | 冷启动至“上次索引展示” | 验证缓存是否让用户先看到可操作界面。 |
| MediaStore 时间 | query 开始至 cursor 遍历结束 | 区分系统媒体库成本。 |
| SAF 枚举时间 | 每个根目录 `walk()` 开始至结束、目录数、文件数 | 明确哪个目录或提供者是最大瓶颈。 |
| 元数据时间 | 新文件数、`MediaMetadataRetriever` 总耗时、失败数 | 确认是否真的是标签读取拖慢扫描。 |
| 匹配时间 | 索引命中次数、全库回退次数、候选数、每个页面耗时 | 量化全库评分是否为扫描后的 CPU 主因。 |
| 首屏帧时间 | 本地页首次打开与快速滚动中的慢帧/冻结帧 | 区分封面读取与 Compose 重组问题。 |
| 重扫次数 | 每次进程启动、授权、设置恢复、下载完成的扫描发起/实际执行数 | 验证重复扫描是否存在。 |

## 五、推荐的改造顺序

| 阶段 | 改造 | 预期收益 | 是否需要 Rust |
|---|---|---|---|
| A：先诊断 | 加分段计时、扫描原因标识和慢帧采样；在真实设备采集一次完整 trace。 | 把“感觉慢”变为可验证的具体段。 | 否 |
| B：消除重复和全量工作 | 启动扫描合并为一次协调任务；下载完成只做单文件并入；无目录变化时跳过 SAF 完整扫描。 | 大幅减少无意义重扫。 | 否 |
| C：SAF 增量索引 | 维护目录快照/文件清单；仅对变化目录枚举与读取。对于没有可靠变更通知的 Provider，采用手动刷新或低频后台校验。 | 解决大目录热扫依然慢的核心问题。 | 否 |
| D：匹配引擎改造 | 删除列表路径的全库回退；持久化倒排索引；把模糊匹配限于小候选集、按需后台执行。 | 消除扫描后 CPU 持续占满和锁竞争。 | 否 |
| E：封面调度 | 首屏只读取可视曲目的封面；以队列限制、磁盘缓存命中优先和占位骨架屏避免滚动时并发 retriever 峰值。 | 改善首屏与滚动帧率。 | 否 |
| F：再评估 Rust | 若 trace 明确显示匹配/规范化是纯 CPU 热点，再迁移单独的匹配核心。 | 对极大库提供额外 CPU 余量。 | 仅此模块可考虑 |

## 六、下一步建议

下一步不应直接重写语言，而应先实施“**可测量的扫描管线**”：为每段扫描、匹配和封面提取添加耗时、文件数、候选数与扫描原因；然后在一台存在问题的真实设备上完成一次冷启动、热扫描和滚动采样。只要结果确认 SAF 枚举或全库匹配占主导，就可以按阶段 B—D 改造，并以指标验证每一项是否真正降低耗时和慢帧。

在未完成上述测量前，Rust 只能被视作**纯匹配模块的备选优化手段**，而不是当前扫描慢与界面卡顿的根治方案。

## 代码证据索引

| 文件 | 关键位置 |
|---|---|
| `MainActivity.kt` | `95-98`、`137-140`：启动、授权与目录恢复的扫描触发。 |
| `MainViewModel.kt` | `132-156`：下载任务变化；`175-228`：扫描调度；`676-736`：匹配索引与全库回退。 |
| `LocalMusicRepository.kt` | `16-80`：MediaStore 全量查询。 |
| `CustomFolderRepository.kt` | `19-67`：SAF 全量递归枚举与元数据读取。 |
| `ArtworkStore.kt` | `36-56`：首屏/滚动时的内嵌封面读取。 |
| `PlaybackConnection.kt` | `73-78`、`211-243`：500ms 播放器快照发布。 |
| `TrackMatcher.kt` | `69-179`、`237-250`：候选评分与 Levenshtein 相似度。 |

---

**本报告未改动任何业务源码。**
